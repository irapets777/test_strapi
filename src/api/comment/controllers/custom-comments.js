// const { createCoreController } = require("@strapi/strapi").factories;

// const moduleCommentId = "api::comment.comment";

// module.exports = createCoreController(moduleCommentId, ({ strapi }) => ({
//     test: async (ctx) => {
//         console.log("TEST");
//     },

// }));